# 模式与脚本编排简要说明

## 结论

可行，且比要求用户始终手填固定表单更适合小说工作台。

核心边界是：**大模型负责理解、追问和补齐结构化输入；脚本负责按固定规则生成与校验文件。** 模型不应绕过脚本直接任意改写项目资产。

```text
用户自然语言
→ 大模型提取字段 / 追问缺失项
→ 结构化脚本输入（可预览）
→ 脚本生成标准产物
→ 校验、确认、进入下一阶段
```

例如“生成角色卡”脚本可定义固定输入：

```json
{
  "name": "唐三",
  "martialSoul": "蓝银草",
  "father": "唐昊",
  "traits": ["冷静", "坚韧", "执着"]
}
```

用户只需说“唐三的武魂是蓝银草，父亲唐昊，冷静坚韧执着”。模型将其转换为上述字段；缺少姓名、身份或关键冲突信息时，模型再以简短问题或选项向用户确认。脚本随后输出规定格式的 `角色卡.md`。

## 脚本契约

每个脚本应声明四项内容：

| 项目 | 说明 |
| --- | --- |
| 输入 Schema | 必填字段、可选字段、类型、枚举值和默认值 |
| 输出 Schema | 产物文件、固定章节或 Markdown 模板 |
| 前置依赖 | 必须已确认的资产，例如正文必须依赖提示词快照 |
| 校验规则 | 缺字段、格式错误、设定冲突、失败时如何回退 |

模型输出的应是 `script_input`，而不是最终文件文本。调用脚本前应展示输入预览，并做 Schema 校验；校验失败时由模型继续追问或让用户手改。

## 澄清请求机制

模型在以下情况不应猜测后继续生成，而应发起澄清请求：

- 脚本必填字段缺失，例如角色卡没有姓名或身份。
- 用户表述存在歧义，例如“他父亲是唐昊”无法判断“他”指向哪个角色。
- 用户输入与已确认的世界观、角色卡或时间线冲突。
- 有多个合理创作分支，但选择会明显影响后续剧情或资产。

澄清请求有两种形式：

1. **开放问题**：请用户补充缺失信息，例如“唐三在当前阶段的身份是什么？”
2. **受限选项**：提供 2 至 4 个可解释选项，例如“本章冲突更适合：A. 当面冲突；B. 误会升级；C. 暗中调查？”

请求中应说明“为什么需要这项信息”“它会影响哪个脚本字段或后续阶段”，并允许用户自行填写“其他”。用户回答前，脚本保持在“等待用户输入”状态，不写入产物，也不进入下一步骤。

```text
模型发现不确定项
→ 创建澄清请求（问题 / 选项 / 影响范围）
→ 用户回答或选择
→ 更新 script_input
→ Schema 校验
→ 调用脚本
```

## 三种模式

### 手动模式

用户直接填写脚本表单，例如姓名、身高、武魂、关系、性格。前端实时校验必填项，填写完成后调用脚本。

- 适合熟悉创作流程、希望精确控制字段的用户。
- 模型不主动补全、不自动进入下一阶段。
- 仍可提供“检查格式”按钮，但不代替用户填写。

### 标准模式（默认）

用户用自然语言提供基础信息，模型将其转换为脚本输入；信息不足时，模型主动追问或给出少量可选项。脚本生成产物后，由用户决定撤回、重试、修改或进入下一步。

- 适合大多数用户。
- 保留生成前的输入预览和生成后的确认关卡。
- 非校验步骤显示：`撤回 / 重试 / 下一步`。
- 校验与验收显示：`撤回 / 重试 / 通过 / 上一步`；只有“通过”后才能进入更新和下一章。

### 自动模式

模型负责收集信息、调用脚本、校验结果，并根据前置依赖与校验结果决定是否进入下一阶段。

- 信息不确定、存在冲突或关键字段缺失时，模型必须暂停并向用户提问。
- 校验失败时，模型只重做相关步骤，不重跑全流程。
- 仍应保留运行记录、输入快照、产物版本和停止按钮。
- 对删除、覆盖、跳过关键必做资产等高影响操作，应要求用户确认；自动模式不等于无条件自动写入。

## 与现有流程的对应

| 阶段 | 标准模式中的模型职责 | 脚本职责 |
| --- | --- | --- |
| 角色卡 | 从对话提取人物字段，追问缺失信息 | 按模板生成角色卡 |
| 剧情书 / 剧情卷 | 将想法整理为卷级、章级字段 | 生成规划文档 |
| 本章设定 / 配置 / 台词 | 把自然语言意图转换为各步骤参数 | 生成对应章节资产 |
| 最终提示词 | 汇总资产来源，提示冲突或缺失 | 编译并冻结提示词快照 |
| 正文 | 提供受约束的章节输入 | 调用正文生成脚本并保存版本 |
| 校验与更新 | 解释问题、提出修复或更新建议 | 校验一致性并产生变更提议 |

## 最小实现顺序

1. 为“角色卡生成”定义 JSON Schema、表单和脚本输入预览。
2. 在标准模式中实现“对话 → 字段提取 → 缺失追问 → 用户确认 → 调用脚本”。
3. 复用该框架到设定、配置、台词、大纲和正文。
4. 最后加入自动模式的阶段判定、停止、日志和人工兜底。

这样既保留自然语言交互的低门槛，也保证每个产物的格式、依赖和可追溯性。
